sumologic:
  useExporterBatching: true
  nodeSelector:
    disktype: hdd
  metrics:
    remoteWriteProxy:
      enabled: true
      config:
        clientBodyBufferSize: "32k"
        port: 80
        enableAccessLogs: true
