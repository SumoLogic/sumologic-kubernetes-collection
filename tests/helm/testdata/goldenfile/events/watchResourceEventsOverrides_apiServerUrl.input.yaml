sumologic:
  events:
    provider: fluentd
fluentd:
  apiServerUrl: "https://kubernetes.default.svc:443"
  events:
    enabled: true
    watchResourceEventsOverrides:
      pods: "v2"
      events: "events.k8s.io/v3"
