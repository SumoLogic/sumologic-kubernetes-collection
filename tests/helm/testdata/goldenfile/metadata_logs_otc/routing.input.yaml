sumologic:
  logs:
    collector:
      otelcol:
        enabled: true
    otelcol:
      extraExporters:
        sumologic/test:
          endpoint: https://test-endpoint
        debug:
          verbosity: detailed
      useDefaultExporters: false
      routing:
        fallbackExporters:
          - debug
          - sumologic/test
        table:
          - exporter: sumologic/test
            context: resource
            condition: attributes["exporter"] == "test"
