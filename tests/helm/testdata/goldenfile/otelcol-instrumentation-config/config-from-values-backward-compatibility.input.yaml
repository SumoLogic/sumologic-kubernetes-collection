otelcolInstrumentation:
  config:
    merge:
    override:
    receivers:
      otlp:
        protocols:
          grpc:
            endpoint: "localhost:4317"
          http:
            endpoint: "localhost:4318"
