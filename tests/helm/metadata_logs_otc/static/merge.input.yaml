sumologic:
  logs:
    metadata:
      provider: otelcol
    collector:
      otelcol:
        enabled: true

fluent-bit:
  enabled: false

metadata:
  logs:
    config:
      merge:
        processors:
          batch:
            send_batch_size: 7
