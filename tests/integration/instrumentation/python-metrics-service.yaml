apiVersion: v1
kind: Service
metadata:
  name: python-metrics-svc
  labels:
    app: python-metrics-svc
spec:
  selector:
    app: python-metrics-app
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
