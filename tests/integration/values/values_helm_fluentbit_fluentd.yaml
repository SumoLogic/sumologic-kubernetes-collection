sumologic:
  events:
    provider: fluentd
  logs:
    collector:
      otelcol:
        enabled: false
    metadata:
      provider: fluentd
  metrics:
    metadata:
      provider: fluentd
  traces:
    enabled: false

fluent-bit:
  enabled: true
