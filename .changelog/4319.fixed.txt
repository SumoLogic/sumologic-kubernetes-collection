fix: bump Sumo Logic Terraform provider to `>= 3.2.5` so collector fields are assigned in the initial create request, fixing collector creation for principals whose ABAC permissions are scoped to those fields
