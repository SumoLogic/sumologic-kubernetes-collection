{{- $fields := concat .Values.sumologic.logs.fields .Values.sumologic.logs.additionalFields -}}
{{- $collectorFields := merge (dict "sumo.cluster.name" .Values.sumologic.clusterName "sumo.helm.release" .Chart.Version "sumo.hostedCollector.subType" "K8s") (.Values.sumologic.collector.fields | default dict) -}}
{{- $variables := dict "fields" $fields "collector_fields" $collectorFields -}}
{{- toPrettyJson $variables | nindent 2 }}
